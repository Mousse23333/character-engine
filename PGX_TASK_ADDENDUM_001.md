# PGX Task Addendum 001 — 范式之争暂缓，平行评估两条路

**日期**：2026-04-27
**作者**：架构师（人类）
**触发**：你前一夜的 EXECUTIVE_SUMMARY / ARCHITECT_DECISIONS 给出的 AC-1.1 抉择问题
**优先级**：本文档**临时高于** `PGX_OVERNIGHT_CAPABILITY_SURVEY.md`，请先消化本文档再继续

---

## 0. 我对你前一夜调研的回应

调研质量非常高。特别是：
- 5 条事实纠错（F1-F5）我全部接受，已开始改 spec
- vLLM-as-Judge 的发现是惊喜，纳入项目基础设施
- DWpose 在二次元上失败的实证非常重要
- Wan 2.2 Animate-14B 的发现重塑了产线 ⑦

**对你的核心问题（"AC-1.1: Animation Asset 是 2D 还是 3D"）的回答**：

**不在这一刻回答**。理由：

1. 当前 AI 发展不均衡。**2D 和 3D 路线在不同环节成熟度差异巨大**，预先押注会错过红利
2. 我们的目标是"能实现沙盒 + 装备状态实时合成 + 二次元品质"，而**不是**"必须 3D"
3. 旧 AC-1 是基于**对业界状态的猜测**，没经过实验验证
4. 你已经用一晚证明：**3D 路线在 Animation 环节有严重断裂**；而 2D 路线（Wan 2.2 Animate-14B）**可能直接就是成品**

**结论**：引入新约束 **AC-0**，临时压过 AC-1：

> **AC-0 — 数据驱动选型，不预先押注 2D / 3D 范式**。两条路平行评估直到出现明确赢家。

---

## 1. 新任务：双路径平行评估

请你**同时**摸两条路的当前状态，给我**带数据的对比**而不是建议我选哪条。我自己来选。

---

### 路径 A：3D 路线（沿用 spec 原结构）

按你前夜准备好的 `tools/run_p1_p4.sh` 推进。重点产出：

| 实验 | 产出物要求 |
|------|-----------|
| **E-05** Hunyuan3D 2.0 mesh | 拿 Alice 参考图（或用 IP-Adapter 扩充后的多视图）出 mesh + 贴图。报告：拓扑质量、UV 质量、背面失真、是否能进 retop |
| **E-05b** ⭐**新增** Hunyuan3D 2.5（如果合规可用） | 测试它**集成的绑骨**质量 — 这是你前夜识别的关键节点 |
| **E-07** retop | Instant Meshes / QuadriFlow 跑 E-05 mesh，给出"动画可用性" 评分 |
| **E-08** UniRig（用 Articulation-XL2.0，Rig-XL/VRoid 未发就用现有的） | 即使次优也跑一次，给出"工程可用性"基线 |
| **E-09** Mixamo 离线 FBX + Blender retarget | 端到端跑通：E-05 mesh → E-07 retop → E-08 rig → 套 walk/idle 动画。**重点：穿帮模式与稳定性** |
| **E-14** 开源 NPR 渲染（HoyoToon 等） | 把绑好的角色用绝区零风格 shader 渲染出来 |

**3D 路线核心问题**（请直接答）：
- 端到端"图 → mesh → rigged → 动起来 → NPR 渲染"**今天能不能跑通**？
- 如果不能，**死在哪一步**？
- 如果能，**质量是业余 / 接近专家 / 不可用**？

---

### 路径 B：2D 视频路线（沿用 Wan 2.2 Animate-14B 假设）

这是你前夜识别但没跑的方向。重点产出：

| 实验 | 产出物要求 |
|------|-----------|
| **E-11-Animate** ⭐**新增** Wan 2.2 Animate-14B 端到端 | 输入：Alice 参考图 + 一段开源驱动视频（人类舞蹈 / 走路 / 简单动作 4-5 段）。输出：Alice 在驱动视频姿态下的动画视频 |
| **E-11-Animate-quality** | 量化评测：角色一致性（CLIP-I 跨帧）、画风稳定性、动作合理性、手部 / 面部失真率 |
| **E-11-Animate-controllability** | 测可控性：能不能精确响应"换装备"、"换表情"等？还是只能整体被驱动视频拖着走？ |
| **E-11-Animate-state** ⭐ | **关键测试**：Alice 穿不同装备（用 IP-Adapter 提供装备参考图）能不能在 Animate-14B 输出里保持？这关系到 AC-3（沙盒装备）能否在 2D 路线实现 |
| **E-11-Animate-latency** | 测**生成一段 5 秒视频的真实耗时** — 对应"实时合成"目标的可行性边界 |

**2D 视频路线核心问题**（请直接答）：
- Wan 2.2 Animate-14B 端到端**今天能不能用**？
- 装备 / 状态变化能不能用 Animate-14B 自然处理？
- 离"沙盒可扩展 + 实时合成"的目标差多远？
- 如果它能解决 80% 问题，剩下 20% 是什么？

---

### 路径 C：混合路线（**这个由你决定要不要测**）

如果你测完 A 和 B 之后发现两条都不完美，但**组合起来能成**，请提出混合方案。例如：
- 3D 用于角色主体，2D 视频用于动作演出
- 离线侧用 3D 生成多角度参考图，在线侧用 2D 合成 / 选择
- ...

不强求，但如果你看到，请写。

---

## 2. 评判标准（我用什么决定 AC-0 收口为哪条）

请按以下维度填表，路径 A / 路径 B / 路径 C 各一列：

| 维度 | 评分（1-5）| 证据 |
|------|----------|------|
| **目标可达性** — 沙盒 + 装备 + 状态实时合成 + 二次元品质 | | |
| **当前工程成熟度** | | |
| **AI 模型未来 12 个月演进上限** | | |
| **AC-6 开源合规度** | | |
| **GB10 (我们硬件) 实际可跑** | | |
| **新装备 / 新角色加入的工程代价** | | |
| **角色 / 风格 一致性可控性** | | |
| **从今天到 MVP 的预估工时** | | |
| **核心硬伤 / 红旗数量** | | |

最后每条路给出一个**句子级总评**：
- "今天就可用，明天就能上 MVP"
- "工程化欠缺 X，3 个月可补"
- "依赖 Y 模型未发布，6 个月观望"
- "范式上不可能达到目标，放弃"

---

## 3. 工作模式

- **不需要每个实验都跑到 100% 完美**。够数据让我做选择就停
- **优先深度对比 A vs B 的关键差异点**，而不是堆量
- **明确写"我没测的部分及理由"**，不要伪装跑过
- 仍然遵循前夜的 `EXECUTIVE_SUMMARY.md` + `ARCHITECT_DECISIONS.md` 报告结构
- 这次报告路径：`pgx_reports/2026-04-27-paradigm-comparison/`

---

## 4. 仍然有效的旧约束

- AC-2 / AC-3 / AC-4 / AC-5 / AC-6 / AC-7 全部仍然生效
- 仅 AC-1（3D-only）暂停，被 AC-0 覆盖
- 所有"不付费 SaaS"原则不变
- LLM-as-Judge 继续作为质量评判工具

---

## 5. 我对你的期待 — 不变

> 你不是在替我们干活，你是在替我们**节省时间**。
>
> 一份让架构师 5 分钟做出"AC-0 收口为 A / B / C"决定的报告，比跑出 100GB 中间产物有价值得多。

---

## 6. 阻塞与例外

如果你发现：
- 路径 A 在 GB10 上工程不可能跑通
- 路径 B 在 GB10 上工程不可能跑通
- 两条路的对比毫无意义（例如评判标准本身有问题）

**写 BLOCKING.md 立即停下**。不要勉强凑数据。

---

## 7. 时间预算

下一段调研期：**~12-16 小时**（你这次有完整 GPU 了）。

如果时间不够全跑，优先级：
1. E-11-Animate 端到端跑通 → 决定 B 路是否成立（最高 ROI）
2. E-09 端到端跑通 → 决定 A 路是否成立
3. E-11-Animate-state（装备测试） → 决定 B 路能否解决 AC-3
4. 其他实验

---

**祝调研顺利。这次的目标是"摸清两条路的真实状态"，不是"押宝"。**

— *架构师，2026-04-27*
